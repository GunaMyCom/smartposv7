# Shift Status

After a successful login:

- The system checks whether a **shift is currently running**.

## Outcomes

- If **no shift is running**, the following message is displayed:
  > "No shift is currently running or Please, Open the shift."

- If a **shift is already running**, the following message is shown:
  > "Shift is already running."
