# Application Login

When the application is launched, it prompts the user to enter a **User ID** and **Password**.

- Only authorized users with valid credentials can log in.
- If credentials are incorrect, access is denied.
